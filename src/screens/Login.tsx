import React from 'react';
import {View, Text} from 'react-native';
import {LoginScreenProps} from '../types';

export default function Login({}: LoginScreenProps) {
  return (
    <View>
      <Text>Login</Text>
    </View>
  );
}
