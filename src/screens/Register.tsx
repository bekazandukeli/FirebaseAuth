import React from 'react';
import {View, Text} from 'react-native';
import {RegisterScreenProps} from '../types';

export default function Register({}: RegisterScreenProps) {
  return (
    <View>
      <Text>Register</Text>
    </View>
  );
}
